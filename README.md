# CarryMatch

CarryMatch is a React/Vite application currently undergoing migration away from Base44.

Current status:
- The frontend app in `src/` is being retained.
- Legacy Base44-era backend/platform surfaces still exist in this repo during migration.
- Base44-specific docs and generated artifacts are being cleaned up separately from runtime changes.
